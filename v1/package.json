{
  "name": "akemi-carrd-v1",
  "private": true,
  "version": "1.0.0",
  "description": "QA smoke package for Akemi carrd (v1 - carrd antigo, em produção)",
  "scripts": {
    "qa:smoke": "playwright test --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
